{% extends "base.html.twig" %}

{% block body %}

    <div class="container">
        <div class="form">
            {{ form_start(formVehiculo) }}
                {{form_widget(formVehiculo)}}
                <button class="btn btn-lg btn-primary" type="submit">Enviar</button>
            {{ form_end(formVehiculo) }}
        </div>
    </div>

{% endblock %}